// FIX: Removed the reference to "vite/client" which was causing a "Cannot find type definition file" error.
// This is safe because no Vite-specific client features (like import.meta.env) are used in the application.
